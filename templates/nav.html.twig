<nav class="navbar navbar-expand-lg bg-body-tertiary rounded my-5" aria-label="Bibliotheque navbar">
	<div class="container-fluid">
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample11" aria-controls="navbarsExample11" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse d-lg-flex" id="navbarsExample11">
			<a class="navbar-brand col-lg-3 me-0" href="{{ path('app_home') }}">Bibliotheque en ligne</a>
			<ul class="navbar-nav col-lg-6 justify-content-lg-center">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_book_index') }}">Livres</a>
				</li>
				{% if is_granted('ROLE_USER') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_reservation_index') }}">Reservations</a>
				</li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_user_index') }}">Utilisateurs</a>
				</li>
				{% endif %}
			</ul>
			<div class="d-lg-flex col-lg-3 justify-content-around">
				{% if is_granted('ROLE_USER') %}
					<a class="btn btn-light" href="#">{{ app.user.email }}</a>
					<a class="btn btn-light" href="{{ path('app_logout') }}">Se déconnecter</a>
				{% else %}
					<a class="btn btn-primary" href="{{ path('app_login') }}">Se connecter</a>
					<a class="btn btn-light" href="{{ path('app_register') }}">S'enregistrer</a>
				{% endif %}

			</div>
		</div>
	</div>
</nav>
